class Product {
  constructor(id, oid, title, imageURL, description, price) {
    this.id = id;
    this.oid = oid;
    this.title = title;

    this.imageURL = imageURL;

    this.description = description;
    this.price = price;
  }
}
export default Product;
